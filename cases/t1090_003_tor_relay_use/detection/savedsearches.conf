[Tor / Multi-hop Proxy — Distinct Relay Count]
search             = `tor_relay_use`
disabled           = 0
is_scheduled       = 1
cron_schedule      = */15 * * * *
dispatch.earliest_time = -2h
dispatch.latest_time   = now
description        = Detects Tor client activity by counting distinct connections to known relay IPs from app/lookups/tor_relays.csv. ATT&CK T1090.003.
action.email       = 0
alert.suppress     = 1
alert.suppress.fields = src
alert.suppress.period = 60m
alert.severity     = 4
alert_type         = number of events
alert_threshold    = 0
alert_comparator   = greater than
counttype          = number of events
quantity           = 0
relation           = greater than
