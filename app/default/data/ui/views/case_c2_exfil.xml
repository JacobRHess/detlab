<dashboard version="1.1" theme="dark">
  <label>T1041 — Exfiltration Over C2 Channel</label>
  <description>Chained detection — fires when a beaconing (src, dest) pair (from the T1071.001 rule) also moves >= 100 KB uplink. Risk-accumulation across two signals.</description>

  <fieldset submitButton="false" autoRun="true">
    <input type="time" token="time">
      <label>Time range</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
    <input type="text" token="src_filter" searchWhenChanged="true">
      <label>Source IP</label>
      <default>*</default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>Active C2 exfil alerts</title>
      <single>
        <search>
          <query>`c2_exfil`
| where like(src, "$src_filter$") OR "$src_filter$"="*"
| stats count</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="colorBy">value</option>
        <option name="rangeValues">[0,1,3]</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
    <panel>
      <title>Total uplink exfiltrated</title>
      <single>
        <search>
          <query>`c2_exfil`
| where like(src, "$src_filter$") OR "$src_filter$"="*"
| stats sum(total_orig_bytes) AS bytes
| eval bytes_human = round(bytes / 1024, 1) . " KB"
| table bytes_human</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </single>
    </panel>
    <panel>
      <title>Distinct C2 destinations</title>
      <single>
        <search>
          <query>`c2_exfil`
| where like(src, "$src_filter$") OR "$src_filter$"="*"
| stats dc(dest)</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>Active alerts</title>
      <table>
        <search>
          <query>`c2_exfil`
| where like(src, "$src_filter$") OR "$src_filter$"="*"
| eval window=strftime(_time, "%Y-%m-%d %H:%M")
| eval bytes_kb=round(total_orig_bytes/1024, 1)
| sort -total_orig_bytes
| table window, src, dest, bytes_kb, exfil_record_count, beacon_connection_count, beacon_avg_interval_seconds</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="count">25</option>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Beacon vs exfil — both signals over time</title>
      <chart>
        <search>
          <query>index=zeek sourcetype=zeek:conn
| where like(src, "$src_filter$") OR "$src_filter$"="*"
| timechart span=15m count AS beacon_conns, sum(eval(if(orig_bytes >= 10000, orig_bytes, 0))) AS uplink_bytes</query>
          <earliest>$time.earliest$</earliest>
          <latest>$time.latest$</latest>
        </search>
        <option name="charting.chart">line</option>
        <option name="charting.axisY2.enabled">true</option>
      </chart>
    </panel>
  </row>

  <row>
    <panel>
      <title>About this detection</title>
      <html>
        <h3>T1041 — Exfiltration Over C2 Channel</h3>
        <p>
          Operators reuse the established C2 connection to push files,
          credentials, and database exports — the destination is already
          allow-listed because the beacon's been talking for hours. The
          detection composes two signals: <strong>beaconing</strong>
          (the T1071.001 rule's output) and <strong>large
          uplink bytes</strong> on the same connection.
        </p>
        <p>
          Splunk does composition via the <code>join type=inner</code>
          subsearch in the macro. The "risk accumulation" pattern this
          implements is what Splunk Enterprise Security shops use in
          practice: behavioural + volumetric = high-confidence alert.
        </p>
        <p>
          Tuning + false-positive notes:
          <a href="https://github.com/JacobRHess/detlab/blob/main/cases/t1041_exfil_over_c2/README.md" target="_blank">cases/t1041_exfil_over_c2/README.md</a>
        </p>
      </html>
    </panel>
  </row>
</dashboard>
